<?php
if (! defined('SY_APP_DIR')) {
    die();
}

/**
 * Amplify tables prefix. Default is "amp_". Boolean false is empty.
 */
// $config['amp:tables_prefix'] = 'amp_';

/**
 * Admin routes prefix. Default is "/admin". Boolean false is empty.  
 */
// $config['amp:prefix'] = '/admin';
